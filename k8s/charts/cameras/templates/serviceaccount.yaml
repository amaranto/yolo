{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  namespace: {{ .Values.env }}
  name: {{ .Values.serviceAccount.name | default (printf "%s-service-account" .Chart.Name) }}
{{- end }}
